### open_v1

```c++
/**
 * Opens a project at the given path. If a project does not exist at that path, it will be created.
 * 
 * @param path The path of the project to open or create.
 * @param flags The flags to use when opening or creating the project.
 * @return A pointer to the opened project or NULL if there was an error.
 */
```